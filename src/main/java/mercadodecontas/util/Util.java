package mercadodecontas.util;

public class Util {
	public static void redirecionarPagina(String pagina) {
		/*
		 * FacesContext context = FacesContext.getCurrentInstance(); String url =
		 * context.getExternalContext().getRequestContextPath(); try {
		 * context.getExternalContext().redirect(url + "/" + pagina); } catch
		 * (IOException ex) { Logger.getLogger(Util.class.getName()).log(Level.SEVERE,
		 * null, ex); }
		 */
	}
}